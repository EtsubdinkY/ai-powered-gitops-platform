name: Terraform Validation

on:
  push:
    branches:
      - main

jobs:
  validate:
    runs-on: [self-hosted, platform-runner]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Terraform version
        run: terraform version

      - name: Google Cloud CLI Version
        run: gcloud version